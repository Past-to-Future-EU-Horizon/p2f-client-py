[project]
name = "p2f-client-py"
version = "0.0.1"
description = "API Client library for the Past to Future project."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "python-dotenv>=1.1.0",
    "requests>=2.32.3",
]
